interface:
  display_name: "Update Dependency Batch"
  short_description: "Discover, propose, and apply CommonEx dependency update bundles"
  default_prompt: "Use $update-dependency-batch to research updates, then after my selection apply and validate only selected versions plus explicitly described Adopt-now or required-migration actions."
