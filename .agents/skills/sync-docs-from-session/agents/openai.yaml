interface:
  display_name: "Sync Docs From Session"
  short_description: "Sync docs from session, including behavior/policy changes"
  default_prompt: "Use $sync-docs-from-session to draft doc updates from this session, including inferred behavior changes from code/test diffs."
