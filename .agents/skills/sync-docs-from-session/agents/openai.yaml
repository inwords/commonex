interface:
  display_name: "Sync Docs From Session"
  short_description: "Propose doc and instruction updates from session evidence, capture agent behavior signals, auto-store high-confidence items, and ask on suggested changes"
  default_prompt: "Use $sync-docs-from-session to propose doc and instruction updates from this session, capture agent behavior signals, auto-store high-confidence changes after the proposal, and ask me which suggested changes to apply."
