interface:
  display_name: "Sync Docs From Session"
  short_description: "Propose doc sync updates from session evidence with Tier A/Tier B handling"
  default_prompt: "Use $sync-docs-from-session to propose doc updates from this session, auto-apply Tier A items after the proposal, and wait for confirmation before Tier B."
